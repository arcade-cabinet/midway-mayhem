node_modules
dist
dist-ssr
*.local

.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

coverage
.nyc_output
.vitest-cache
playwright-report
playwright/.cache
test-results
.cr

android/app/build
android/build
android/.gradle
android/local.properties
ios/App/build
ios/App/Pods
ios/App/App.xcworkspace/xcuserdata
ios/DerivedData

.env
.env.local
.env.*.local

.claude/state/
!.claude/state/.gitkeep
.claude/worktrees/
.claude/scheduled_tasks.lock

# Project-local worktrees
.worktrees/

# Reference conversations (kept locally, not tracked in repo)
ChatGPT-Clown_Car_3D_Prototype.md
Gemini-Conversation.md
ChatGPT-*.md
*-Conversation.md

# Session notes
.remember/

# Vitest browser test artifacts
.vitest-attachments/
**/__screenshots__/
.test-screenshots/
# …but DO commit pinned baselines so visual-regression tests can compare
# against them in CI. Phase 4 of the cockpit hero pass lives here.
!**/__baselines__/
!**/__baselines__/**

# Debug pause-and-capture output
.capture/

# Output of scripts/build-cockpit-prototype.py — renders + blueprint.
# The canonical blueprint lives at src/config/cockpit-blueprint.json;
# the Blender re-render is for reference only, never committed.
.cockpit-prototype/

# Ad-hoc Playwright MCP snapshots (console + page dumps) + stray
# screenshots from live-debugging sessions — never checked in.
.playwright-mcp/
/*.png

# v1 codebase moved here for reference; not a live module
# (lint/tsc/tests all exclude it)

# copywasm output (copied from node_modules/sql.js/dist by pre-hooks)
public/sql-wasm.wasm
public/sql-wasm-browser.wasm
public/assets/sql-wasm.wasm
public/assets/sql-wasm-browser.wasm
